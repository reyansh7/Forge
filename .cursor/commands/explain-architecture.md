Follow the `forge-architecture` skill.

Summarize what the source-of-truth documents actually contain. If `docs/ARCHITECTURE.md`, `docs/ROADMAP.md`, or `docs/DEVELOPMENT_RULES.md` are empty, say so clearly. Do not invent architecture.

You may delegate to the `architect` subagent. Do not implement anything.
